version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb+srv://admin:1234@cluster-todolist.9l6jcyo.mongodb.net/todoDB?retryWrites=true&w=majority&appName=Cluster-todoList
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    restart: unless-stopped
